---
name: travel-guide
description: 制作中文旅游攻略（逐日行程 + 酒店 + 美食 + 单文件可分享网页）。当用户要做旅游攻略、行程规划、亲子游/自由行路线、几日游安排、酒店比价、机票高铁比价、必吃美食清单，或要把行程做成小红书风格的可分享网页/链接时使用。触发词：旅游攻略、行程、路线规划、自由行、亲子游、几日游、酒店推荐、民宿、必吃美食、攻略网页、旅游计划书。
agent_created: true
---

# 旅游攻略生成器（Travel Guide Builder）

把"机票 + 酒店 + 逐日行程 + 美食"整理成一份**手机上能看、能分享给家人**的单文件网页攻略，并发布成链接。

配套模板：`assets/travel-guide-template.html`（一套可直接改数据的完整成品骨架，含倒计时、路线图、逐日时间轴、今天吃什么、待办清单）。

---

## 一、六条铁律

1. **先定行程，再做计划书。** 行程没敲定前不要动手写 HTML。用户一定会改，早写就是白写。
2. **一天一天确认，不要一次给全套。** 逐日推进：今天定 D3，明天定 D4。用户明确要求过"先把行程确定下来再去做计划书"。
3. **数据必须有来源。** 价格、时刻、营业时间要能追溯到携程 / 12306 / 官方小程序 / 搜索引擎侧的社媒笔记。查不到就说查不到，不要编。
4. **兼顾同行所有人。** 若行程带商务、采购、看货等个人目的，住宿和动线必须同时满足老人小孩的景点与吃饭需求——否则会被骂"只考虑自己"。
5. **酒店比的是"实付含什么"，不是裸房价。** 含早几份、含不含门票/接送、免费取消截止时间，这四项经常让贵 10 元的选项实际更划算。
6. **餐饮不要偷懒。** 别把三餐全排在酒店（五星酒店餐食不便宜，且家人会觉得敷衍）。落地日尤其要给出酒店周边街区的平价选择。

---

## 二、流程

### 阶段 0 · 信息收集（先问，别猜）
必问清单：
- 出发城市 / 目的地 / 日期区间 / 几大几小 / 孩子年龄
- 已出票的交通（航班号 + 时刻），还是要代查
- **硬约束**：必须去的景点、必须办的事、忌口、体力上限
- 住宿偏好：酒店还是民宿、要不要泳池/儿童设施、预算档位

### 阶段 1 · 方案对比
给 2 个方案（例："只玩一地" vs "两地联游"），各列：天数分配、交通成本、体力强度、适合谁。**给出明确推荐 + 理由**，等用户选。

### 阶段 2 · 交通
- 机票：直飞/经停、准点率、**是否含餐食和免费托运行李额**（低成本航司常不含，返程带着采购品会超重）
- 高铁：12306 预售期 15 天，给车次号 + 时刻 + 票价
- 落地到酒店：车程、打车价、有无接驳
- 早班机：确认出发前一晚的安排。若住机场周边酒店，**核实接送班车是否需提前预约、最早班次能否对上航班**

### 阶段 3 · 住宿（逐晚定）
每晚给 1 主推 + 1 备选，必写：
- 房型 / **实付价** / **含早几份** / 含不含门票或接送 / **免费取消截止时间**
- 与次日行程的距离和动线

### 阶段 4 · 逐日行程（核心交付）
每天 = 时间轴 + 三餐 + 住宿，并满足：
- 时间轴留缓冲，**硬约束单独标注**（停售时间、末班船、预约截止、摇号时间）
- 有预约/票务门槛的，写清入口名称 + 放票时间（例：「屿见厦门」小程序每天 9:00 放第 15 天票）
- 每天附一条备用方案（`alt`）："若 X 抢不到就改 Y"

### 阶段 5 · 美食
- 先按城市汇总，再**拆分到对应日期**
- 每家给：店名 / 招牌菜 / 人均 / **营业时间** ← 本地小吃的最大坑（很多只做早市、有的 13:00 就售罄、有的 16:30 才开）
- 标注"售罄时间/打烊时间"，避免扑空

### 阶段 6 · 落地成网页 + 发布
单文件 `index.html`，无外部依赖，手机优先（`max-width:760px`）。

**数据结构**（直接套模板）：
```js
var DAYS = [
  { d:'01', w:'10月1日 周四', t:'大标题', s:'副标题',
    f:'住 xxx 酒店（含早 2 份）',
    stops:[{tm:'07:40', n:'事件名', d:'说明，可用 <b> 强调', nav:'高德搜索关键词，没有则 null'}],
    eat:[{w:'中午', n:'店名', p:'¥20–40', d:'招牌菜 / 备注'}],
    alt:'若 A 失败的备选方案'
  }
];
```
- `nav` 非空 → 渲染导航按钮 `https://uri.amap.com/search?keyword=<encodeURIComponent(nav)>`
- `eat` → 渲染「🍜 今天吃什么」卡片；`alt` → 渲染「💡 提醒」
- 页面模块顺序：hero + 倒计时 → 概览 → 路线 SVG → 航班卡片 → 住宿 → 交通 → 逐日（默认展开 D1）→ 城市美食 → 出行贴士 → 待办清单（localStorage 勾选）
- 视觉：日夜双配色（`prefers-color-scheme`），主色海蓝 `#1b7fbd`、强调橙 `#e4744a`，圆角卡片 + 轻阴影
- 待办清单用 `localStorage` 持久化，key 要带版本号

**发布**：用 sites 能力（`workbuddy_sites_deploy`），`directory` = 项目目录，`domainPrefix` = 英文短 slug（如 `xiamen-chaoshan-7d`）。
⚠️ **只有用户当轮明确说"发布/上线/覆盖"才可调用**；改了内容后再次发布需用户重新确认，同意不跨轮次生效。

---

## 三、质量校验（必做，别省）

**1. JS 语法**
```bash
node -e "const h=require('fs').readFileSync('index.html','utf8');
[...h.matchAll(/<script>([\s\S]*?)<\/script>/g)].forEach((x,i)=>{
  try{ new Function(x[1]); console.log('#'+i+' OK'); }catch(e){ console.log('#'+i+' ERR '+e.message); }});"
```

**2. 真机渲染截图**（只看代码容易漏掉渲染问题）
```bash
chrome --headless --remote-debugging-port=9333 --user-data-dir=C:/tmp/cr-hl "file:///<绝对路径，中文需 URL 编码>" &
```
再用 CDP：`Emulation.setDeviceMetricsOverride{width:440, mobile:true}` → 算目标元素文档坐标 → `Page.captureScreenshot{format:'png', captureBeyondViewport:true, clip:{x,y,width,height}}`。
长卡片**分两段截**，否则缩放后看不清文字。

---

## 四、取数技巧（实战验证过）

| 需求 | 可行做法 |
|---|---|
| 读用户已订的携程订单 | **`https://my.ctrip.com/` 是唯一稳定入口**。PC 端 `hotels.ctrip.com/hotels/orderlist`、`/member/hotelorder`、`/hotels/myorder` **全返回空白页**（正文仅 52 字）。订单详情 `hotels.ctrip.com/hotels/ctorderdetail?orderid=<id>` 可正常读 |
| 拿 orderid | 在 `my.ctrip.com` 上 `querySelectorAll('a[href]')`，**「取消」按钮的 href 里带 `orderid=xxx`** |
| 携程页面点不动 | 面包屑/导航是 SPA 路由，派发 DOM 事件与 CDP 真实鼠标点击（`Input.dispatchMouseEvent`）**都可能不跳转** → 直接找 URL，别硬点 |
| 小红书笔记数据 | **不要直连小红书**（未登录 + 海外 IP 会报 `300012 IP存在风险`）。改用搜索引擎检索 `xiaohongshu.com + 关键词`，拿二次聚合内容（今日头条/搜狐/去哪儿/携程旅拍），店名、招牌菜、人均、营业时间都齐 |
| 机票价格 | **移动版页面 + 价格日历**比 PC 端稳；PC 端对自动化常返空白页 |
| 网页取数通用 | 连用户已登录的真实 Chrome（`--remote-debugging-port=9222`）比新开实例强：登录态、Cookie 都在。复制 Cookie 到新实例通常无效（Chrome 127+ 应用绑定加密 + 设备风控） |

---

## 五、踩坑清单

- **Write 长文件会被截断**：单次 Write 超过约 60–70KB 有风险（1000+ 行 HTML 会在 70% 处硬截断）。大文件分段写，写完必须 `tail` 校验尾部完整（`</script></body></html>`）。
- **`cd X && cmd &` 会把 `cd` 一起后台化**，后续命令 cwd 没变 → 脚本一律用绝对路径。
- **儿童设施免费与否要交叉核实**：不同来源会互相矛盾（"含儿童乐园" vs "¥198/位"）。能打电话确认就别靠页面文案。
- **景点预约类信息单独列清单**：南普陀/厦大摇号/船票预售/索道停售，每条都要有入口名 + 时间点。
- **酒店"含早"要问清份数**：2 大 1 小只含 2 份早，孩子超 1.2m 常需另付。
- **别编价格**：拿不准的写区间 + 来源，或直接留空让用户打电话确认。

---

## 六、交付形态

1. `index.html`（本地，可直接双击打开）
2. 线上分享链接（用户同意发布后）
3. 同步更新网页里的**待办清单**：把所有需要用户亲自打电话/抢票的事项置顶，标 `hot:true`

记住：用户要的不是一份漂亮文档，是**出发那天能照着走、不会踩坑的东西**。
